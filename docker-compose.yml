services:
  api:
    build: .
    network_mode: host
    env_file:
      - .env
    volumes:
      - .:/app
    command: sh -c "php -S 0.0.0.0:$${APP_PORT} -t public public/index.php"
    restart: unless-stopped
